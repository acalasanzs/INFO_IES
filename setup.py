from cx_Freeze import setup, Executable

setup(
    name = "AMPLIACIÓ 4 EXCELENT BY ACS",
    version = "0.1",
    desciption = "Idk",
    executables = [Executable("AMPLIACIÓ 4 EXCELENT BY ACS.py")],
)